{
  "name": "battlefield-2042-loading-spinner",
  "description": "A recreation of the Battlefield 2042 loading spinner in HTML / CSS.",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/dominique-mueller/battlefield-2042-loading-spinner"
  },
  "type": "module",
  "scripts": {
    "build": "rimraf ./dist && parcel build ./index.html --public-url ./",
    "start": "rimraf ./dist && parcel ./index.html"
  },
  "devDependencies": {
    "parcel-bundler": "1.12.x",
    "rimraf": "3.0.x"
  }
}
